import { environment } from "../../../environments/environment";

export const API_URL = environment.API_PATH;
export const API_PATH = {
    AUTH: 'auth/',
    ROLE: 'role/',
    USER: 'user/',
    PC_LIST: 'pclist/'
}